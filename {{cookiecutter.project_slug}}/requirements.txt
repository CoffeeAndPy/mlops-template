# Base dependencies for MLOps projects.
# Add project-specific ones below each section.
#
# Best practices:
#   - Pin exact versions (==) in production for reproducible builds.
#   - Use >= only for dev libraries where compatibility matters less.
#   - Separate runtime dependencies from dev/CI ones.

# Data and ML
numpy==1.26.4
pandas==2.2.1
matplotlib==3.8.3
scikit-learn==1.4.2
#TODO: add project-specific libraries here
# pytorch, transformers, xgboost, etc.

# MLOps
mlflow==3.10.1
dvc==3.49.0

# API
fastapi==0.110.0
uvicorn[standard]==0.27.1
pydantic==2.6.3
prometheus-client==0.20.0

# Notebooks
jupyter==1.0.0
ipykernel==6.29.3

# Utils
requests==2.31.0
python-dotenv==1.0.1

# Dev / CI (not included in production Docker image)
pytest==8.1.0
pytest-cov==5.0.0
ruff==0.3.4